# Plano — Radar v3

Spec: `docs/superpowers/specs/2026-07-21-radar-v3.md`.
Gaps: `docs/superpowers/specs/2026-07-21-auditoria-gaps.md`.

Ordem pensada para a suíte ficar verde a cada task. TDD nas funções puras.
Commits por escopo, PT-BR, autor Pedro.

## Task 1 — Fundações de dados (puras)
- [ ] `scraper/util.ts`: fim do dia em `-03:00` (DAD-6) + `cortarEmPalavra`
- [ ] `scraper/classificador.ts`: tecnologia sem `inovacao/inovador*`
      (DAD-2); sustentab com colocação ambiental (DAD-8); testes novos
      (inclui ORG-5)
- [ ] `scraper/schema.ts`: `publicoAlvo?`, `fluxoContinuo?`
- [ ] `scraper/mesclar.ts`: preserva `coletadoEm` (DAD-3), dedupe de
      conteúdo (DAD-10), normaliza `situacao` (DAD-11) + testes

## Task 2 — Fontes
- [ ] FINEP: `dataDePublicacao`, descarte de mortos >18 meses (DAD-1),
      `fluxoContinuo`, descrição 600 em palavra (DAD-7), `publicoAlvo`
      persistido (DAD-4) + fixtures/testes
- [ ] CNPq: página de detalhe → descrição + reclassificação (DAD-5) +
      fixture nova
- [ ] FAPEG: reclassificação com texto do PDF já baixado (DAD-9) + teste

## Task 3 — Lib do front (puras)
- [ ] `limparTitulo` v2 com os 3 títulos reais (TXT-1)
- [ ] `EFPC` em SIGLAS (TXT-4)
- [ ] Prefixo-título removido da descrição (TXT-2)
- [ ] `filtrar`: termo curto por palavra inteira; "ia" casa a flag (FUN-1)
- [ ] `listarAreasDisponiveis` extraída (TEC-5)
- [ ] `lerAreas` saneada contra áreas válidas (FUN-2)
- [ ] `lib/ics.ts` (FUN-4)
- [ ] Helper de frescor ("atualizado hoje/ontem/há N dias") (FUN-3)
- [ ] `tests/http.test.ts` (ORG-2) e fronteiras 7/8/30/31 (ORG-3)

## Task 4 — Front & design
- [ ] `globals.css`: tokens ajustados, espinha com nós, transições
- [ ] `layout.tsx`: metadata OG/twitter/metadataBase + viewport/themeColor
- [ ] `icon.svg`, `apple-icon`, `manifest.ts`, `robots.ts`, `sitemap.ts`,
      `opengraph-image.tsx` (+ fontes em `assets/`)
- [ ] `page.tsx`: `DadosSchema.parse` (TEC-1)
- [ ] `Controles.tsx`: barra nova sticky, busca visível, alvos de toque,
      "minhas áreas" (UI-1/3.3, UI-7, TXT-5)
- [ ] `GrupoPrazo.tsx`: cabeçalho editorial, contador legível, espinha,
      vazio de "Esta semana" (UI-3/4/5)
- [ ] `LinhaEdital.tsx`: stretched link, hover, nó, `.ics`, "novo",
      público-alvo, fluxo contínuo, peso no crítico (1.4, CRIT-3)
- [ ] `Radar.tsx`: frescor no header, filtros na URL, última visita,
      View Transitions, reuso do GrupoPrazo no sem-prazo (TEC-3)
- [ ] `npm i lucide-react`

## Task 5 — CI, docs e higiene
- [ ] `.github/workflows/ci.yml` (ORG-1)
- [ ] Cron: `timeout-minutes`, `concurrency`, rebase (ORG-4, CRIT-4)
- [ ] README: FAPEG/PDF, CNPq detalhe, aviso IA (TXT-6, DAD-12)
- [ ] Remover SVGs órfãos; notas nas docs de 20/07 (ORG-6)

## Task 6 — Verificação
- [ ] `npm test` + lint + `tsc --noEmit` + `npm run build`
- [ ] `npm run scrape` real + medições (mortos fora, áreas re-medidas,
      descrições CNPq, dedupe, coletadoEm estável)
- [ ] Screenshots 4 combos vs. "antes"
- [ ] Workflow de revisão multi-lente sobre o diff
- [ ] Commits por escopo
